/* Canonical link to the Google Business Profile listing. CID form, which is
   stable — unlike the coordinate URLs Maps produces when you share from the
   map view. Shared by the hero rating and the reviews section. */
export const GOOGLE_REVIEWS_URL = "https://www.google.com/maps?cid=17576216227421438205";

/* The rating shown beside the stars. This must match the live listing: it is
   a factual claim about a real rating, so if the listing average ever moves
   off 5.0, change it here rather than leaving five stars painted on. */
export const GOOGLE_RATING = "5.0";
